import { Text, View } from "react-native";

const Configuration = () => {
    return (
        <View>
            <Text>Essa é a tela de configurações de Dispositivos</Text>
        </View>
    );
}

export default Configuration;